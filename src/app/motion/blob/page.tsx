import React from 'react';
import BlobPage from "@/components/UI/blob-page";

const Page = () => {
    return (
        <div className={"w-screen h-screen bg-[#363861] absolute inset-0"}>
            <BlobPage />
        </div>
    );
};

export default Page;